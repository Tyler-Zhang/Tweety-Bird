To start the server, run "node server.js"


To test using the test client, cd into /server, open the Node.js REPL, and type in:
"var test = require('./test_client.js')"

Run "test.sendTestPost(strKeyword)", replacing strKeyword with a string

